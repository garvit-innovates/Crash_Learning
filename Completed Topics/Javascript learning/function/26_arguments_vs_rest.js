// Q26. Create a function named showArgs using rest parameters (...args).
// Print all received arguments.
// Call it with "Garvit", 25, "Tester".

function showArgs(...args) {
    console.log(args);
}

showArgs("Garvit", 25, "Tester");