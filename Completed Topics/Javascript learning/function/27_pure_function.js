// Q27. Create a pure function named add that accepts two numbers
// and returns their sum without changing any outside variable.
// Call it with 10 and 20 and print the result.

function add(a,b){
    return a + b 
};

console.log(add(10,20));