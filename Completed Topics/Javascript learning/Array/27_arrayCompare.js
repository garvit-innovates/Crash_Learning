// Q27. Create two arrays with the same values and check whether they are equal using a suitable approach.

let num1 = [1,2,3];
let num2 = [1,2,3];

// console.log(num1 === num2);
console.log(num1 == num2);

// let result = num1.length === num2.length &&
//              num1.every((value, index) => value === num2[index]);

// console.log(result);