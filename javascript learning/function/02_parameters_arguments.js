// Q2. Create a function named greet that accepts a name as a parameter
// and prints "Hello <name>".
// Call it with "Garvit" as the argument.

function greet(name){
    console.log(`Hello ${name}`);
}
greet("garvit")