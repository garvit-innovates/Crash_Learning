// Q19. Create an object named user with name: "Garvit".
// Try to access user.address.city safely using optional chaining (?.).
// Print the result.

let user = {
    name: "Garvit"
};

console.log(user.address?.city);