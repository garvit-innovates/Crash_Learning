/*

================ ARRAY QUICK REVISION ================

push()       → add element at end
pop()        → remove element from end
unshift()    → add element at beginning
shift()      → remove element from beginning

length       → number of elements
indexOf()     → find index
includes()    → check whether value exists

slice()      → extract part, original unchanged
splice()     → add/remove/replace, modifies original

join()       → array → string
concat()     → combine arrays
reverse()    → reverse array
sort()       → sort array

forEach()    → execute function for every element
map()        → transform elements → new array
filter()     → select elements → new array
find()       → first matching element
findIndex()  → index of first matching element
some()       → at least one matches
every()      → all match
reduce()     → array → single value

...           → spread / copy / combine arrays
destructuring → extract array values

Important:
Array index starts from 0.
Last index = array.length - 1.

shift()      → remove element from beginning

length        → number of elements
indexOf()     → find index
includes()    → check whether value exists

slice()      → extract part, original unchanged
splice()     → add/remove/replace, modifies original

join()       → array → string
concat()     → combine arrays
reverse()    → reverse array
sort()       → sort array

forEach()    → execute function for every element
map()        → transform elements → new array
filter()     → select elements → new array
find()       → first matching element
findIndex()  → index of first matching element
some()       → at least one matches
every()      → all match
reduce()     → array → single value

...           → spread / copy / combine arrays
destructuring → extract array values

Important:
Array index starts from 0.
Last index = array.length - 1.

*/