// Q2. Create an array of 3 fruits, add one fruit at the end using push(), and remove the last fruit using pop().

let fruits = ["apple", "banana", "orange", "grapes"];

fruits.push("kiwi");
fruits.pop();

console.log(fruits);