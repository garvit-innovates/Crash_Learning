// Q24. Create one array and one string, then check which value is an array using Array.isArray().

let nums = [1, 2, 3, 4, 5, 6];

let string = "garvit";

console.log(Array.isArray(nums));
console.log(Array.isArray(string));